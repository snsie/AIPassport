# Core Libraries
# The floor is real, not nominal: st.navigation() needs >=1.36, the activity pickers
# need st.segmented_control, and every chart and table now sizes with width="stretch"
# rather than the use_container_width= argument Streamlit deprecated with a removal
# date of 2025-12-31. That argument is gone or going; width= is what replaced it, and
# it does not exist on older releases. Verified end to end against 1.61.1 via
# scripts/verify_notebooks.py, so that is the floor rather than a guess at the exact
# release where width= landed.
streamlit>=1.61.0
pandas
numpy
matplotlib
seaborn
scikit-learn
scikit-image
opencv-python-headless
statsmodels
plotly
altair
google-genai>=1.0.0
openai
streamlit-timeline
Faker

# System & Utils
psutil
GitPython
tenacity>=8.0.0
requests
Pillow
pydeck
pyarrow

# Model Explainability
shap
lime
