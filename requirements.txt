# Core Libraries
# NOTE: the >=1.31.0 floor below is understated and predates this file's history.
# The entrypoint's st.navigation() needs >=1.36, and the notebooks use newer APIs
# still (st.segmented_control, bordered columns, st.spinner(show_time=...)).
# Verified end to end against streamlit 1.61.1 via scripts/verify_notebooks.py.
# Raising the floor is a deployment decision, so it is left as-is rather than guessed.
streamlit>=1.31.0
pandas
numpy
matplotlib
seaborn
scikit-learn
scikit-image
opencv-python-headless
statsmodels
plotly
altair
google-genai>=1.0.0
openai
streamlit-timeline
Faker

# System & Utils
psutil
GitPython
tenacity>=8.0.0
requests
Pillow
pydeck
pyarrow

# Model Explainability
shap
lime
